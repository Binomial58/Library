# 運用方針

このリポジトリは継続的に整理・追加・書き換えを行う個人ライブラリです。作業のたびに同じ判断をやり直さなくて済むよう、踏むべき手順をここにまとめておきます。

## 1. 新しいコードを追加するとき

- 自作のコードは、既存の命名規則・ドキュメント形式に合わせる
  - C++: `{name}.hpp` + 同名の `{name}.md`（`documentation_of` フロントマター付き）のペア、カテゴリごとの `README.md` に一覧を追記
  - Python: カテゴリごとのディレクトリに配置し、そのカテゴリの `README.md`（無ければ作成）に一覧を追記
- 他所のアルゴリズム・実装を参考にした／流用した場合は、必ず次節のチェックを行う

## 2. 第三者コードを含めるかどうかの判断

公開リポジトリなので、他人のコードを載せるときは必ず元のライセンスを確認する。

1. **元のリポジトリ・ページに明示的な `LICENSE` ファイルがあるか確認する**
   - GitHub の `LICENSE` タブ、リポジトリ直下の `LICENSE`/`LICENSE.md`/`COPYING` を直接見る（README の記述だけでは不十分。無いのに「MITだと思う」で進めない）
2. **ある場合**：そのライセンスの条件を守った上で `LICENSE` ファイルごとこのリポジトリに含めてよい
   - Public domain / Unlicense / CC0：制限なし。表示義務も無いが、出典を残すのが望ましい（例: `python/SortedSet`）
   - MIT / BSD：`LICENSE` ファイルと著作権表示をそのまま残す（例: `vendor/ac-library`。ac-library 本体は CC0 だが、同じ「必ず LICENSE ファイルを同梱する」という扱いをしている）
   - GPL 系（GPL/LGPL/AGPL）：copyleft のため、組み込むとリポジトリ全体がそのライセンスに従う必要が出てくる。基本的に避ける
3. **無い場合**（`LICENSE` が存在しない = デフォルトで著作権者に全ての権利が留保される）：このリポジトリ（公開）には含めない。選択肢は次の3つ：
   1. 使わない
   2. `.gitignore` に追加してローカルにだけ残す（公開リポジトリの追跡対象から外す。理由をコメントで残す）
   3. 同じアルゴリズムを自分で一から書き直す
      - 変数名を変える・整形し直す程度では不十分（元コードの二次的著作物のまま）
      - 実装方針・コード構造を実際に考え直して書く。著作権が保護するのは「具体的な表現」であって「アルゴリズムそのもの」ではないため、独立した実装であれば元コードの二次的著作物にはならない
      - 既に `library/cpp/` にある自作実装がある領域（segtree, union_find, SCC, topological_sort 等）は、その設計をそのまま Python に移植すれば実質的にゼロから考え直す必要はない

## 3. 「無許諾で公開してしまっていた」ことに後から気づいた場合

1. まず `git rm -r --cached` で該当ファイルを git 管理から外し、`.gitignore` に追加してコミット・push する（当面のリスクをすぐ止める）
2. コミット履歴自体からも消したい場合は履歴の書き換えが必要（**破壊的操作なので実行前に必ずユーザーに確認する**）
   - コミット数が少ない・他の履歴を残す必要が無い場合：現在の状態を単一の orphan コミットとして作り直し、古いブランチと差し替えて `git push --force` する（このリポジトリでの実例: 2コミットしか無かったため、この方法で履歴を1コミットに作り直した）
   - コミット数が多い・特定パスだけ除去して他の履歴は残したい場合：`git-filter-repo` を使う（このマシンには未インストール。必要ならその場でインストール可否を確認する）
   - 実施後はローカルの `git reflog expire --expire=now --all` + `git gc --prune=now --aggressive` で、ローカルの古いオブジェクトも掃除する
   - GitHub 側が force push 前のコミットをどの程度キャッシュ保持するかはこちらで制御できない旨を、作業後にユーザーへ正直に伝える

## 4. ディレクトリ・ファイルの整理をするとき

- 移動は `git mv` を使い、リネーム履歴を保つ（`git status --short` で `R` になっているか確認）
- 移動・書き換え後は動作確認する
  - C++: `g++ -std=c++20 -fsyntax-only` でコンパイル確認
  - Python: `python3 -m py_compile` で構文確認 — ただし `__pycache__/` が生成されるので、確認後に消す・`.gitignore` に入れておく（一度、確認作業中に生成された `__pycache__` を誤ってそのままコミットしてしまったことがある）
- 命名規則を変える場合（大文字小文字・ハイフン/アンダースコアなど）は、対応する `.md` ドキュメントの記述（順序・見出し）も同時に確認し、ズレていれば直す
- 内容の欠落・重複が無いことを機械的に確認してからコミットする（例: 並び替えなら `diff <(sort old) <(sort new)` が空になるか、宣言のシグネチャ一覧が前後で一致するか）

## 5. まだ未対応の既知の課題

- `cpp/` の自作コード自体に明示的な `LICENSE` が無い（デフォルトで全著作権留保の状態）。他の人が安心して参照・流用できるようにするなら、MIT や CC0 などのライセンスを付けることを検討する（ユーザーの判断待ち）
